# @tooling/env

## loadEnv

Loads environment variables from `.env*` files and puts them into `process.env` object.

## parseEnv

Parses the given env object using Zod schema and returns the result

## CLI

```shell
$ copy-env
```

Copies `.env*` files from monorepo root to the project directory
