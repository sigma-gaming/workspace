export enum UserType {
  Authenticated = 'Authenticated',
  Guest = 'Guest',
  Unknown = 'Unknown',
}

export enum EmitScope {
  Socket = 'Socket',
  Global = 'Global',
  Local = 'Local',
  User = 'User',
  UserOptimized = 'UserOptimized',
}
